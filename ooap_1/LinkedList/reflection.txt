Рефлексия на выполнение задания №2 (LinkedList)

Мною был добавлен АТД для Node и его сигнатуру использовал внутри АТД LinkedList, не знаю корректно ли это
или нет, но мне показалось, что это хороший подход.

Ошибки:
1. Забыл указать постусловия, а надо было.
2. Некорректно указал предусловия у некоторых методов, в следствие чего не все статусы и запросы
статусов учёл.
3. Некорректно указал предусловие "список не пустой" для clear, remove_all, find так как считал что лучше вернуть
статус с ошибкой (особенно для find). Оказывается был не прав.

Успехи:
1. Корректно ответил на все доп. вопросы
2. Корректно описал сигнатуру методов (если учесть сигнатуру Node корректной)

ИТОГО: проделал работу над ошибками, ознакомился с тем как надо было сделать, поправил свою версию.