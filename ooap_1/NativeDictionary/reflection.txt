Рефлексия на выполнение задания №8 (NativeDictionary)

1. Не добавил метод is_exist(). А он пригодится в реализации, чтобы не проверять статус запроса get().
2. Добавил maxSize, что является в корне не верным, так как АТД не знает о внутренней реализации, и, возможно, этот параметр будет не нужным
